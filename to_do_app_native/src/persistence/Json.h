#pragma once
// This header includes the nlohmann::json single-header downloaded by FetchContent.
// CMake adds the ${nlohmann_json_SOURCE_DIR} to include paths, which contains json.hpp.
#include "json.hpp"
